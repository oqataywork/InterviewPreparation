1) ƒелегат - специальный объект, хран€щий в себе указатель на метод
2)ƒелегат можно называть класс-делегат
3)Ёкземпл€р класса-делегата может ссылатьс€ на любой статический или экземпл€рный метод, при условии, что их их сигнатуры совпадают
4)ƒелегат - безопасный указатель на метод
5)¬  ласс-делегате строго указан тип возвращаемого значени€ метода, с которого он может быть сообщен
6)ƒелегат - это специальный объект, котоырй содержит в себе указатели на первый адрес байта, с которого начинаетс€ метод в пам€ти

[байт][x1][x2][x3][байт][байт][байт]
x - метод в пам€ти
ƒелегат содержит в указатель на байт x1

[access modifier] delegate [type] [Name] ([type] name);
private delegate int NumberDelegate(int counter);

Inheritance of Delegate Class:
NumberDelegate - MulticastDelegate - Delegate - Object

Delegate Usage:

MyDelegate mD = new MyDelegate(method())

md.Invoke();

//or

md();
